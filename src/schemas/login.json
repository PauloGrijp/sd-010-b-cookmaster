{
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "format": "email"
    },
    "password": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": ["email", "password"],
  "errorMessage": {
    "required": "All fields must be filled",
    "_": "Invalid entries. Try again."
  }
}
