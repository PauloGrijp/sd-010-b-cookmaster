// const app = require('./app');

// const PORT = 3000;

// app.listen(PORT, () => console.log(`conectado na porta ${PORT}`));

const app = require('./app');

const PORT = 3000;

app.listen(PORT, () => console.log(`conectado na porta ${PORT}`));
